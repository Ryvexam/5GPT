version: '3.8'

services:
  app:
    container_name: toolswithai-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      # Add any other environment variables here if needed
      # - OPENAI_API_KEY=sk-...
